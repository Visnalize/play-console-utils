{
  "editor.formatOnSave": true,
  // Tailwind's at-rules (@import 'tailwindcss', @plugin, @source, @apply) aren't
  // known to VS Code's built-in CSS language service.
  "css.lint.unknownAtRules": "ignore",
  "explorer.sortOrder": "type"
}
