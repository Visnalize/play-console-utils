# Generated files — `pnpm format` reformatting these produces thousands of lines
# of pure churn and, for the changelog, fights the release workflow that writes it.
pnpm-lock.yaml
docs/changelog.md
# Regenerated wholesale by `pnpm ppp:refresh`; reformatting it would just make
# the next refresh look like a 200-line diff.
utils/ppp-data.ts

# Build output
.output
.wxt
docs/.vitepress/dist
docs/.vitepress/cache
